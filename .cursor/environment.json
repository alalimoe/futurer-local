{
  "install": "node --version && npm --version",
  "env": {
    "GOOGLE_CLIENT_ID": {
      "description": "Google OAuth Client ID for support@nootropix.shop Gmail access"
    },
    "GOOGLE_CLIENT_SECRET": {
      "description": "Google OAuth Client Secret"
    }
  }
}
