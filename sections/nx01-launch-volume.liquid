{% render 'nx01-launch-css' %}

<section
  id="nx01-volume"
  class="nx01 nx01-volume xo-section xo-section--{{ section.id }}{% if section.settings.color_scheme != blank %} color-{{ section.settings.color_scheme }}{% endif %}"
  aria-labelledby="nx01-volume-heading-{{ section.id }}"
>
  <div class="nx01__inner">
    <div class="nx01__intro">
      {%- if section.settings.eyebrow != blank -%}
        <p class="nx01__label">
          <span class="nx01__label-rule" aria-hidden="true"></span>
          <span>{{ section.settings.eyebrow | escape }}</span>
        </p>
      {%- endif -%}
      {%- if section.settings.heading != blank -%}
        <h2 class="nx01__heading" id="nx01-volume-heading-{{ section.id }}">{{ section.settings.heading | escape }}</h2>
      {%- endif -%}
      {%- if section.settings.subheading != blank -%}
        <p class="nx01__sub">{{ section.settings.subheading | escape }}</p>
      {%- endif -%}
    </div>

    <div class="nx01-volume__split">
      <div class="nx01-volume__graphic">
        <div class="nx01-volume__col">
          {%- if section.settings.one_label != blank -%}
            <p class="nx01-volume__cap">{{ section.settings.one_label | escape }}</p>
          {%- endif -%}
          <div class="nx01-volume__pill nx01-volume__pill--single" aria-hidden="true">
            <span class="nx01-volume__fill nx01-volume__fill--low"></span>
          </div>
          {%- if section.settings.one_value != blank -%}
            <p class="nx01-volume__value">{{ section.settings.one_value | escape }}</p>
          {%- endif -%}
          {%- if section.settings.one_note != blank -%}
            <p class="nx01-volume__note">{{ section.settings.one_note | escape }}</p>
          {%- endif -%}
        </div>
        <p class="nx01-volume__vs">{{ 'sections.nx01_launch_volume.vs' | t }}</p>
        <div class="nx01-volume__col nx01-volume__col--nx">
          {%- if section.settings.three_label != blank -%}
            <p class="nx01-volume__cap">{{ section.settings.three_label | escape }}</p>
          {%- endif -%}
          <div class="nx01-volume__pills" aria-hidden="true">
            <span class="nx01-volume__pill"><span class="nx01-volume__fill"></span></span>
            <span class="nx01-volume__pill"><span class="nx01-volume__fill"></span></span>
            <span class="nx01-volume__pill"><span class="nx01-volume__fill"></span></span>
          </div>
          {%- if section.settings.three_value != blank -%}
            <p class="nx01-volume__value">{{ section.settings.three_value | escape }}</p>
          {%- endif -%}
          {%- if section.settings.three_note != blank -%}
            <p class="nx01-volume__note">{{ section.settings.three_note | escape }}</p>
          {%- endif -%}
        </div>
      </div>

      <div class="nx01-volume__copy">
        {%- if section.settings.statement != blank -%}
          <p class="nx01-volume__statement">{{ section.settings.statement | escape }}</p>
        {%- endif -%}
        {%- if section.settings.body != blank -%}
          <div class="nx01-volume__body">{{ section.settings.body }}</div>
        {%- endif -%}
      </div>
    </div>
  </div>
</section>

{% style %}
  .nx01-volume__split {
    display: grid;
    grid-template-columns: 1fr;
    gap: 4.8rem;
    align-items: center;
  }

  .nx01-volume__graphic {
    display: flex;
    flex-direction: column;
    flex-wrap: nowrap;
    align-items: center;
    justify-content: center;
    gap: 2.4rem;
    padding: 3.2rem 1.6rem;
    background: rgb(var(--nx01-surface));
    border-radius: var(--nx01-radius);
  }

  .nx01-volume__col {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1.6rem;
    text-align: center;
    min-width: 0;
    width: 100%;
  }

  .nx01-volume__col:not(.nx01-volume__col--nx) {
    padding-top: 2.4rem;
  }

  .nx01-volume__col--nx {
    padding: 2.4rem;
    border-radius: var(--nx01-radius);
  }

  .nx01-volume__cap {
    margin: 0;
    font-size: 1.2rem;
    font-weight: 600;
    letter-spacing: 0.1em;
    text-transform: uppercase;
  }

  .nx01-volume__vs {
    margin: 0;
    font-size: 1.8rem;
    font-weight: 600;
  }

  .nx01-volume__pill {
    display: flex;
    flex-direction: column;
    justify-content: flex-end;
    width: 5.6rem;
    height: 12rem;
    padding: 0.3rem;
    overflow: hidden;
    border: 0.2rem solid rgb(var(--nx01-ink));
    border-radius: 2.8rem;
  }

  .nx01-volume__pill--single {
    position: relative;
    overflow: visible;
  }

  .nx01-volume__pills {
    display: flex;
    gap: 1.2rem;
  }

  .nx01-volume__fill {
    display: block;
    width: 100%;
    height: 8.8rem;
    border-radius: 0.2rem 0.2rem 2.4rem 2.4rem;
    background: rgb(var(--nx01-ink));
    opacity: 0.8;
  }

  .nx01-volume__fill--low {
    height: 16rem;
    background: rgb(var(--nx01-ink));
    opacity: 0.28;
    border-radius: 0.4rem 0.4rem 2.4rem 2.4rem;
  }

  .nx01-volume__value,
  .nx01-volume__note,
  .nx01-volume__statement {
    margin: 0;
  }

  .nx01-volume__value {
    font-size: 1.4rem;
    font-weight: 600;
  }

  .nx01-volume__note {
    font-size: 1.2rem;
  }

  .nx01-volume__statement {
    font-size: 2.4rem;
    font-weight: 600;
    line-height: 1.3;
  }

  .nx01-volume__body {
    margin-top: 2.4rem;
    font-size: 1.5rem;
    line-height: 1.7;
    color: rgb(var(--nx01-body));
  }

  .nx01-volume__body p {
    margin: 0 0 2.4rem;
  }

  .nx01-volume__body p:last-child {
    margin-bottom: 0;
  }

  @media screen and (min-width: 600px) {
    .nx01-volume__graphic {
      flex-direction: row;
      flex-wrap: nowrap;
      padding: 4rem 2.4rem;
    }

    .nx01-volume__col {
      min-width: 16rem;
      width: auto;
    }
  }

  @media screen and (min-width: 1200px) {
    .nx01-volume__split {
      grid-template-columns: minmax(0, 1.2fr) minmax(0, 1fr);
      gap: 8rem;
    }
  }
{% endstyle %}

{% schema %}
{
  "name": "NX-01 launch volume",
  "tag": "section",
  "class": "section-nx01-volume",
  "enabled_on": {
    "templates": ["page"]
  },
  "settings": [
    { "type": "color_scheme", "id": "color_scheme", "label": "Color scheme", "default": "background-1" },
    { "type": "text", "id": "eyebrow", "label": "Eyebrow", "default": "06 · Volumetric Integrity" },
    { "type": "text", "id": "heading", "label": "Heading", "default": "The listed amounts need three capsules." },
    { "type": "textarea", "id": "subheading", "label": "Subheading", "default": "A standard capsule cannot hold the full daily serving. We use three so the label and the serving match." },
    { "type": "text", "id": "statement", "label": "Statement", "default": "Three capsules exist so we do not have to shrink the formula." },
    { "type": "richtext", "id": "body", "label": "Body", "default": "<p>The nine actives add up to about 1,950 mg per daily serving. That will not fit in one standard capsule without cutting every amount.</p><p>So the ingredient table is written for three capsules, not one. One bottle is 30 of those full servings.</p>" },
    { "type": "text", "id": "one_label", "label": "One-capsule label", "default": "One capsule" },
    { "type": "text", "id": "one_value", "label": "One-capsule value", "default": "The listed serving would not fit" },
    { "type": "text", "id": "one_note", "label": "One-capsule note", "default": "You would have to cut every amount" },
    { "type": "text", "id": "three_label", "label": "Three-capsule label", "default": "NX-01 daily serving" },
    { "type": "text", "id": "three_value", "label": "Three-capsule value", "default": "The listed amounts, as written" },
    { "type": "text", "id": "three_note", "label": "Three-capsule note", "default": "9 actives across 3 capsules" }
  ],
  "presets": [
    { "name": "NX-01 launch volume" }
  ]
}
{% endschema %}
